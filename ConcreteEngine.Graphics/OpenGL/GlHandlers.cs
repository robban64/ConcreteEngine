namespace ConcreteEngine.Graphics.OpenGL;

internal readonly record struct GlTextureHandle(uint Handle);

internal readonly record struct GlShaderHandle(uint Handle);

internal readonly record struct GlMeshHandle(uint Handle);

internal readonly record struct GlVertexBufferHandle(uint Handle);

internal readonly record struct GlIndexBufferHandle(uint Handle);

internal readonly record struct GlFrameBufferHandle(uint Handle);

internal readonly record struct GlRenderBufferHandle(uint Handle);